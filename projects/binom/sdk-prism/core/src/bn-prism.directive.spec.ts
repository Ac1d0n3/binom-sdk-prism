import { BnPrismDirective } from './bn-prism.directive';

describe('BnPrismDirective', () => {
  it('should create an instance', () => {
    const directive = new BnPrismDirective();
    expect(directive).toBeTruthy();
  });
});
